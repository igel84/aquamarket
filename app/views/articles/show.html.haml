- content_for :head do
  %title= @article.title  
  %meta{name:"keywords", content:"#{@article.keywords == nil ? @article.title : @article.keywords}"}
  %meta{name:"description", content:"#{@article.keywords == nil ? @article.title : @article.keywords}"}
%h1!= @article.title
!= @article.body